<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Predicate rule instead of the class-based dictionary: the
			     dictionary's File/Image/Movie/Text/WebURL classes miss
			     documents delivered as in-memory data (e.g. a PDF viewed in
			     Safari), so Cylonix never appeared in the share sheet for
			     them. Accept any item set whose attachments all provide a
			     data, URL, or text representation. -->
			<key>NSExtensionActivationRule</key>
			<string>SUBQUERY (
    extensionItems,
    $extensionItem,
    SUBQUERY (
        $extensionItem.attachments,
        $attachment,
        ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.data"
        OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.url"
        OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.text"
    ).@count == $extensionItem.attachments.@count
    AND $extensionItem.attachments.@count &gt;= 1
    AND $extensionItem.attachments.@count &lt;= 100
).@count &gt;= 1</string>
			<key>PHSupportedMediaTypes</key>
			<array>
				<string>Video</string>
				<string>Image</string>
			</array>
		</dict>
		<key>NSExtensionMainStoryboard</key>
		<string>MainInterface</string>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
	</dict>
	<key>AppGroupId</key>
	<string>group.io.cylonix.sase.ios</string>
</dict>
</plist>
